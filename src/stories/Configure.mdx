import { Meta } from "@storybook/addon-docs/blocks";

import Github from "./assets/github.svg";
import Discord from "./assets/discord.svg";
import Youtube from "./assets/youtube.svg";
import Tutorials from "./assets/tutorials.svg";
import Styling from "./assets/styling.png";
import Context from "./assets/context.png";
import Assets from "./assets/assets.png";
import Edit from "./assets/edit.png";
import Html from "./assets/html.png";
import Combined from "./assets/combined.png";
import Testing from "./assets/new.png";
import Accessibility from "./assets/accessibility.png";
import Theming from "./assets/theming.png";
import AddonLibrary from "./assets/addon-library.png";

export const RightArrow = () => <svg 
    viewBox="0 0 14 14" 
    width="8px" 
    height="14px" 
    style={{ 
      marginLeft: '4px',
      display: 'inline-block',
      shapeRendering: 'inherit',
      verticalAlign: 'middle',
      fill: 'currentColor',
      'path fill': 'currentColor'
    }}
>
  <path d="m11.1 7.35-5.5 5.5a.5.5 0 0 1-.7-.7L10.04 7 4.9 1.85a.5.5 0 1 1 .7-.7l5.5 5.5c.2.2.2.5 0 .7Z" />
</svg>

<Meta title="Welcome" />

<div className="sb-container">
  <div className='sb-section-title'>
    # Welcome to The Entertainer Code Design System

    Using this tool you can edit and view components to produce the HTML and CSS required for each component to then use on site. You can also view different states of each component to see how they will look in different scenarios.
  </div>
</div>
<div className="sb-container">
  <div className='sb-section-title'>
    # How To Use This Tool
  </div>

  <div className="sb-section">
    <div className="sb-features-grid">
      <div className="sb-grid-item">
        <img src={Edit} alt="A screenshot showing the editing of a story" />
        <h4 className="sb-section-item-heading">Editing</h4>
        <p className="sb-section-item-paragraph">Each component is listed in the menu, simply choose your component. Once loaded you will see the options available for that component. Editing these options will update the component in real-time, allowing you to customize it to your needs.</p>
      </div>
      <div className="sb-grid-item">
        <img src={Html} alt="A browser window showing a Storybook created HTML code" />
        <h4 className="sb-section-item-heading">Created HTML</h4>
        <p className="sb-section-item-paragraph">Once you are finished editing, switchover to the HTML view to see the generated code. At the very bottom of the panel there is a 'copy' button which can be clicked to copy all of the generated code to your clipboard. <strong>RECOMMENDED:</strong> Save your generated HTML code to a local file for future reference and use. Your amended components will be saved, but backing up is the safest option and gives a reference to previous versions.</p>
      </div>
      <div className="sb-grid-item">
        <img src={Combined} alt="Combining components example code" />
        <h4 className="sb-section-item-heading">Combining Components</h4>
        <p className="sb-section-item-paragraph">Some componrnts are set up in a way so they can be combined. For example the Hero Carousel has input areas for HTML that would be generated generally by the 'Half Split Hero' component and/or the 'Image Hero' component.</p>
        <p className="sb-section-item-paragraph">Usimg the steps detailed above first edit a components HTMl and copy the generated HTML and paste this into the relevant areas e,g 'carouselfirstslide'Finally. using the same steps as individual components, switch to the HTML and copy the generated HTML.</p>
      </div>
      <div className="sb-grid-item">
        <img src={Testing} alt="Screenshot of new" />
        <h4 className="sb-section-item-heading">New Components or Amends Required?</h4>
        <p className="sb-section-item-paragraph">If you believe new components or options should be added to this tool, please contact Dev Ops to get these implemented and published.</p>
      </div>
    </div>
  </div>
</div>

<style>
  {`
  .sb-container {
    margin-bottom: 48px;
  }

  .sb-section {
    width: 100%;
    display: flex;
    flex-direction: row;
    gap: 20px;
  }

  img {
    object-fit: cover;
  }

  .sb-section-title {
    margin-bottom: 32px;
  }

  .sb-section a:not(h1 a, h2 a, h3 a) {
    font-size: 14px;
  }

  .sb-section-item, .sb-grid-item {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .sb-section-item-heading {
    padding-top: 20px !important;
    padding-bottom: 5px !important;
    margin: 0 !important;
  }
  .sb-section-item-paragraph {
    margin: 0;
    padding-bottom: 10px;
  }

  .sb-chevron {
    margin-left: 5px;
  }

  .sb-features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    grid-gap: 32px 20px;
  }

  .sb-socials {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
  }

  .sb-socials p {
    margin-bottom: 10px;
  }

  .sb-explore-image {
    max-height: 32px;
    align-self: flex-start;
  }

  .sb-addon {
    width: 100%;
    display: flex;
    align-items: center;
    position: relative;
    background-color: #EEF3F8;
    border-radius: 5px;
    border: 1px solid rgba(0, 0, 0, 0.05);
    background: #EEF3F8;
    height: 180px;
    margin-bottom: 48px;
    overflow: hidden;
  }

  .sb-addon-text {
    padding-left: 48px;
    max-width: 240px;
  }

  .sb-addon-text h4 {
    padding-top: 0px;
  }

  .sb-addon-img {
    position: absolute;
    left: 345px;
    top: 0;
    height: 100%;
    width: 200%;
    overflow: hidden;
  }

  .sb-addon-img img {
    width: 650px;
    transform: rotate(-15deg);
    margin-left: 40px;
    margin-top: -72px;
    box-shadow: 0 0 1px rgba(255, 255, 255, 0);
    backface-visibility: hidden;
  }

  @media screen and (max-width: 800px) {
    .sb-addon-img {
      left: 300px;
    }
  }

  @media screen and (max-width: 600px) {
    .sb-section {
      flex-direction: column;
    }

    .sb-features-grid {
      grid-template-columns: repeat(1, 1fr);
    }

    .sb-socials {
      grid-template-columns: repeat(2, 1fr);
    }

    .sb-addon {
      height: 280px;
      align-items: flex-start;
      padding-top: 32px;
      overflow: hidden;
    }

    .sb-addon-text {
      padding-left: 24px;
    }

    .sb-addon-img {
      right: 0;
      left: 0;
      top: 130px;
      bottom: 0;
      overflow: hidden;
      height: auto;
      width: 124%;
    }

    .sb-addon-img img {
      width: 1200px;
      transform: rotate(-12deg);
      margin-left: 0;
      margin-top: 48px;
      margin-bottom: -40px;
      margin-left: -24px;
    }
  }
  `}
</style>
